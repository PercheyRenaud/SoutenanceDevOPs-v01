---

  - hosts: mediawiki
    become: true
    roles:
      - role: wp.installation_FS

      # - role:
    vars:
      - livr_dir: /appli/mediawiki/LIVR/28-05
      - project_dir: /appli/projects/MWC/mediawiki
      - package_path: /tmp/INPUT.zip
